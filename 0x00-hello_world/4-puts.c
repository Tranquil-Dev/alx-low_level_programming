#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints a string using put
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Myputs[] = "\"Programming is like building a multilingual puzzle";

	puts(Myputs);
	return (0);
}
