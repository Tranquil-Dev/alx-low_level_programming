#include <stdio.h>

/**
 * main - prints hexadecimals
 *
 * Return: 0 
 */
int main(void)
{
	int num;
	char alpha;

	for (num = 0; n < 17; n++)
	{
		putchar(n);
	}
	for (ch = 'a'; ch <= 'f'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
