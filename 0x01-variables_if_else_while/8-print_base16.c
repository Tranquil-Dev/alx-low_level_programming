#include <stdio.c>

/**
 *  main - print hexadecimals
 *
 *   Return - 0
 */
int main(void)
{
	int num = 0;
	char b;
	
	b = 'z';
	while (num < 16)
	{
		if (a < 10)
			putchar(num + '0');
		else
			putchar(b + (num % 10);
			num++
	}
	putchar('\n');
	return (0);
}
