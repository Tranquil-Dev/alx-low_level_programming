#include <stdio.h>
/**
 * main - A code that prints all single digit numbers
 *
 * Return: 0
 */
int main(void)
{
  int number;
  for (number = 0; number < 10; number++)
    printf("%d", num);
  printf("\n");
  return (0);
}
