#include "main.h"

/**
 * print_alphabet - Print the alphabet x10
 *
 * Return: void.
 */
void print_alphabet_x10(void)
{
	char alpnum;
	int num;

	for (num = 1; nu < 11; num++)
	{
		for (alpnum = 'a'; alpnum <= 'z'; alpnum++)
		{
			_putchar(alpnum);
		}
	}
	_putchar('\n');
}
